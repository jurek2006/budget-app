import React, { Component } from "react";

export class Expenses extends Component {
    render() {
        return (
            <div>
                <h1>Expenses</h1>
            </div>
        );
    }
}

export default Expenses;
